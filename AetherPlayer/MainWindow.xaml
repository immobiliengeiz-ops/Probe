<Window x:Class="AetherPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:AetherPlayer.Converters"
        xmlns:views="clr-namespace:AetherPlayer.Views"
        mc:Ignorable="d"
        Title="Aether Player"
        Width="1480"
        Height="900"
        MinWidth="1200"
        MinHeight="760"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        DragOver="Window_OnDragOver"
        Drop="Window_OnDrop">
    <Window.Resources>
        <conv:EnumMatchConverter x:Key="EnumMatchConverter" />
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="Space" Command="{Binding PlayPauseCommand}" />
        <KeyBinding Modifiers="Control" Key="Right" Command="{Binding NextCommand}" />
        <KeyBinding Modifiers="Control" Key="Left" Command="{Binding PreviousCommand}" />
        <KeyBinding Modifiers="Control" Key="F" Command="{Binding FocusSearchCommand}" />
        <KeyBinding Key="MediaPlayPause" Command="{Binding PlayPauseCommand}" />
        <KeyBinding Key="MediaNextTrack" Command="{Binding NextCommand}" />
        <KeyBinding Key="MediaPreviousTrack" Command="{Binding PreviousCommand}" />
        <KeyBinding Key="Delete" Command="{Binding RemoveQueueTrackCommand}" />
    </Window.InputBindings>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="130" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="370" />
        </Grid.ColumnDefinitions>

        <views:Sidebar Grid.Row="0" Grid.Column="0" Margin="0,0,16,12" />

        <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,16,12">
            <views:HomeView>
                <views:HomeView.Style>
                    <Style TargetType="views:HomeView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=Home}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:HomeView.Style>
            </views:HomeView>

            <views:LibraryView>
                <views:LibraryView.Style>
                    <Style TargetType="views:LibraryView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=Library}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:LibraryView.Style>
            </views:LibraryView>

            <views:PlaylistView>
                <views:PlaylistView.Style>
                    <Style TargetType="views:PlaylistView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=Playlists}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:PlaylistView.Style>
            </views:PlaylistView>

            <views:FavoritesView>
                <views:FavoritesView.Style>
                    <Style TargetType="views:FavoritesView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=Favorites}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:FavoritesView.Style>
            </views:FavoritesView>

            <views:RecentlyAddedView>
                <views:RecentlyAddedView.Style>
                    <Style TargetType="views:RecentlyAddedView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=RecentlyAdded}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:RecentlyAddedView.Style>
            </views:RecentlyAddedView>

            <views:RecentlyPlayedView>
                <views:RecentlyPlayedView.Style>
                    <Style TargetType="views:RecentlyPlayedView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=RecentlyPlayed}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:RecentlyPlayedView.Style>
            </views:RecentlyPlayedView>

            <views:CollectionsView>
                <views:CollectionsView.Style>
                    <Style TargetType="views:CollectionsView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ActiveSection, Converter={StaticResource EnumMatchConverter}, ConverterParameter=Collections}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:CollectionsView.Style>
            </views:CollectionsView>
        </Grid>

        <views:SongDetailPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,12" />

        <views:BottomPlayerBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" />
    </Grid>
</Window>
