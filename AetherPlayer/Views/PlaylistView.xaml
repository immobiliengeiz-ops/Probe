<UserControl x:Class="AetherPlayer.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Playlists" FontSize="30" FontWeight="Bold" Margin="0,0,0,14" />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Playlists}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource PanelCardStyle}" Width="260" Height="220" Margin="0,0,14,14">
                        <StackPanel>
                            <Image Source="{Binding CoverPath, Converter={StaticResource ImagePathFallbackConverter}}" Height="110" Stretch="UniformToFill" />
                            <TextBlock Text="{Binding Name}" Margin="0,10,0,4" FontSize="16" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Description}" Foreground="{StaticResource BrushTextSubtle}" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding TrackCount, StringFormat={}{0} tracks}" Margin="0,8,0,0" Foreground="{StaticResource BrushTextSubtle}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
